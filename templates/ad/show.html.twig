{% extends 'base.html.twig' %}

{% block title %}{{ ad.title }}{% endblock %}

{% block body %}
    <div class="ad-title" style="background-image: url({{ad.coverImage}})">
        <div class="container">
            <h1>{{ad.title}}</h1>
            <p>{{ad.introduction}}</p>
            <p class="h2">
                <strong>{{ad.rooms}} chambres</strong> pour <strong>{{ad.price}}&euro;</strong>par nuit</p>
            <a href="#" class="btn btn-primary">Réserver !</a>

        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ ad.content|raw }}

                <div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="carousel">
                    <div class="carousel-indicators">
                        {% for i in 0..ad.images|length-1 %}
                            <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="{{ i }}" {% if loop.first %}class="active"{% endif %} aria-current="true" aria-label="Slide {{ i+1 }}"></button>
                        {% endfor%}
                   </div>

                    {% if ad.images|length >= 1 %}
                        <div class="carousel-inner">
                            {% for image in ad.images %}
                                <div class="carousel-item {% if loop.first %}active{% endif %}">
                                    <img src="{{ image.url  }}" class="d-block w-100" alt="...">
                                    <div class="carousel-caption d-none d-md-block">
                                        <h5>{{ ad.title }}</h5>
                                        <p>{{ image.caption }}</p>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    {% else %}
                        Aucune image touvée..
                    {% endif %}

                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
            </div>

            <div class="col">
               <div class="row">
                   <div class="col-4">
                       <img src="http://placehold.it/64x64" alt="Avatar de Joseph">
                   </div>
                   <div class="col">
                       <h3>Joseph Dupont</h3>
                       <span class="badge bg-primary">3 annonces</span>
                   </div>
               </div>
                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ad aut cupiditate excepturi fugit ipsum
                    nemo ratione, repellendus unde vitae voluptatibus.</p>
                <p>Accusantium, consequuntur debitis dicta dolor dolores dolorum ducimus error esse natus nemo pariatur
                    quam quisquam, reiciendis sapiente soluta velit, voluptates!</p>
                <p>Aliquid doloremque nam reiciendis! Blanditiis delectus illo laboriosam laudantium magni molestias
                    nobis officiis porro quis repudiandae, rerum sint veritatis voluptas.</p>
            </div>
        </div>


    </div>



{% endblock %}
